import WebSocket from 'ws'; globalThis.WebSocket = WebSocket; import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '../apps/storefront/.env' }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const { data: u, error: e } = await supabase.auth.signUp({ email: 'test_merge@example.com', password: 'password123' }); if(e) return console.log(e); const userId = u.user.id; await supabase.from('cart_items').insert({ user_id: userId, product_id: 'a71b10a2-2ab0-42ec-a0ba-0c1598f4cd5c', quantity: 2 }); const { data, error } = await supabase.from('cart_items').select('quantity, product_id, products(name, price, images, stock)').eq('user_id', userId); console.log('Test result:', JSON.stringify({ data, error }, null, 2)); } run();
